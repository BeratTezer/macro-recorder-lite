# Dev/Build requirements (PyInstaller build)
# Install these in a separate env or with `-r requirements-dev.txt` when building the .exe
pyinstaller==6.15.0
pyinstaller-hooks-contrib==2025.8
altgraph==0.17.4
packaging==25.0
setuptools==80.9.0

# Windows-only helpers for PyInstaller
pefile==2023.2.7 ; platform_system == "Windows"
pywin32-ctypes==0.2.3 ; platform_system == "Windows"
